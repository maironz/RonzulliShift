<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_info"
        android:orderInCategory="100"
        android:icon="@drawable/ic_question_white"
        android:onClick="onClickInfoStats"
        android:title="@string/instructions"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/action_edit_shift"
        android:orderInCategory="100"
        android:icon="@drawable/ic_edit_white"
        android:onClick="onClickEditShift"
        android:title="@string/title_msa"
        app:showAsAction="never" />

</menu>